version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: ./schema.sql
    gen:
      go:
        package: "db"
        out: "db"
        emit_methods_with_db_argument: true
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
